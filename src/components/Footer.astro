---
interface Props {
  texts: {
    aboutTitle: string;
    aboutText: string;
    contactTitle: string;
    rights: string;
  }
}

const { texts } = Astro.props;
const phone = "904-678-1775";
const email = "contact@olverasplumbing.com"; // Correo ejemplo
---

<footer class="bg-olveras-blue text-white py-12 border-t-4 border-olveras-red">
  <div class="container mx-auto px-4">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      
      <div>
        <h3 class="text-2xl font-black mb-4 uppercase tracking-wider">Olveras <span class="text-gray-400">Plumbing</span></h3>
        <p class="text-gray-300 text-sm leading-relaxed max-w-xs">
          {texts.aboutText}
        </p>
      </div>

      <div>
        <h4 class="font-bold text-lg mb-4 text-olveras-silver">{texts.aboutTitle}</h4> <ul class="space-y-2 text-gray-300">
          <li><a href="#services" class="hover:text-white transition">Services</a></li>
          <li><a href="#gallery" class="hover:text-white transition">Gallery</a></li>
          <li><a href="#" class="hover:text-white transition">Reviews</a></li>
        </ul>
      </div>

      <div>
        <h4 class="font-bold text-lg mb-4 text-olveras-silver">{texts.contactTitle}</h4>
        <ul class="space-y-3 text-gray-300">
          <li class="flex items-center gap-3">
            <span>📞</span>
            <a href={`tel:${phone}`} class="hover:text-white font-bold">{phone}</a>
          </li>
          <li class="flex items-center gap-3">
            <span>📧</span>
            <a href={`mailto:${email}`} class="hover:text-white">{email}</a>
          </li>
          <li class="flex items-center gap-3">
            <span>📍</span>
            <span>Jacksonville, FL & Surroundings</span>
          </li>
        </ul>
      </div>

    </div>

    <div class="border-t border-white/10 mt-12 pt-8 text-center text-sm text-gray-500">
      &copy; {new Date().getFullYear()} Olveras Plumbing. {texts.rights}
    </div>
  </div>
</footer>